# api_testing_playwright
npm init -y

# Install dependencies
npm install -D @playwright/test  //test runner + API client
npm install ajv dotenv  //schema validation //environment variables

# Install Playwright
npx playwright install


We will use:

Restful Booker API https://restful-booker.herokuapp.com
